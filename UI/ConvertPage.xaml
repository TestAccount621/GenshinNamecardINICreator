<Page x:Class="GenshinNamecardINICreator.UI.ConvertPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GenshinNamecardINICreator.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="500"
      Title="ConverGIFPage" Background="LightGray" >
    <Page.Resources>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Content="Select Image(s)" Click="Button_Click_Open" Style="{StaticResource BaseButtonStyle}"  Grid.Column="0" Grid.Row="1" />
        <ListView x:Name="lv_SelectedGIFs"
                  Grid.Row="2" Grid.Column="0" Margin="10" 
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  SelectionMode="Extended" 
                  SelectionChanged="lv_SelectedGIFs_SelectionChanged" />
        <StackPanel Orientation="Vertical" Grid.Row="2" Grid.Column="1" VerticalAlignment="Top" Margin="0,10,10,10">
            <Button x:Name="btn_Remove" 
                    Content="Remove"
                    Click="Button_Click_Remove"
                    IsEnabled="False"
                    Style="{StaticResource BaseButtonStyle}">
            </Button>
            <TextBlock Height="10" Focusable="False" IsEnabled="False" />
            <Button Content="Convert" Click="Button_Click_Convert">
                <Button.Style>
                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="IsEnabled" Value="True" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Items.Count, ElementName=lv_SelectedGIFs}" Value="0">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Page>
