<UserControl x:Class="GenshinNamecardINICreator.Components.ProgressBarWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenshinNamecardINICreator.Components"
             mc:Ignorable="d" Background="{StaticResource PrimaryBackground}" FontSize="18">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding TitleText}" Grid.Row="0" Foreground="White" />
        <ProgressBar Value="{Binding CurrentProgress, Mode=OneWay}"
                     Width="400"
                     Height="25"
                     Grid.Row="1"
                     Maximum="1"
                     Minimum="0"
                     Margin="0 10"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0 0 0 10">
            <TextBlock Text="Current Item:" Grid.Row="2" Margin="0 0 15 0" Foreground="White" />
            <TextBlock Text="{Binding CurrentItem}" Width="300" TextWrapping="Wrap" Foreground="White" />
        </StackPanel>
    </Grid>
</UserControl>
