<UserControl x:Class="GenshinNamecardINICreator.Components.ConvertPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenshinNamecardINICreator.Components"
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontSize="18" Background="{StaticResource PrimaryBackground}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>

            <ControlTemplate x:Key="SelectedTemplate" TargetType="ListViewItem">
                <Border CornerRadius="5" BorderThickness="1" BorderBrush="DarkGray" Background="#FF92C6F9" Padding="2" Margin="5" Cursor="Hand">
                    <TextBlock Text="{Binding DisplayName}" />
                </Border>
            </ControlTemplate>

            <Style TargetType="ListViewItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border CornerRadius="5" BorderThickness="1" BorderBrush="DarkGray" Background="WhiteSmoke" Padding="2" Cursor="Hand" >
                                <TextBlock Text="{Binding DisplayName}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsSelected" Value="true" />
                            <!--<Condition Property="Selector.IsSelectionActive" Value="true" />-->
                        </MultiTrigger.Conditions>
                        <Setter Property="Template" Value="{StaticResource SelectedTemplate}" />
                    </MultiTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="10">
            <Button Content="Select Image(s)" Style="{StaticResource BaseButtonStyle}" Command="{Binding SelectImagesCommand}" />
        </StackPanel>
        <ListView x:Name="lv_ImageList" Grid.Row="1" Grid.Column="0" Margin="10 0 10 10" ItemsSource="{Binding Files}" />
        <!--Remove and Convert Buttons-->
        <StackPanel Orientation="Vertical" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Top"
                    Grid.Column="1"
                    Grid.Row="1"
                    Margin="0 0 10 0">
            <Button Content="Remove" Command="{Binding RemoveSelectionCommand}" CommandParameter="{Binding ElementName=lv_ImageList, Path=SelectedItems}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedItems.Count, ElementName=lv_ImageList}" Value="0">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBlock Focusable="False" IsEnabled="False" Height="10" />
            <Button Content="Convert" Command="{Binding ConvertCommand}" CommandParameter="{Binding Files}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource SubmitButtonStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Items.Count, ElementName=lv_ImageList}" Value="0">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
