<UserControl x:Class="GenshinNamecardINICreator.Components.CreateNamecardModPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenshinNamecardINICreator.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource PrimaryBackground}" FontSize="18">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Margin" Value="10" />
            </Style>
            
            <ControlTemplate x:Key="SelectedTemplate" TargetType="ListViewItem">
                <Border CornerRadius="5" BorderThickness="1" BorderBrush="DarkGray" Background="#FF92C6F9" Padding="2" Margin="5" Cursor="Hand">
                    <TextBlock Text="{Binding DisplayName}" />
                </Border>
            </ControlTemplate>
            
            <Style TargetType="ListViewItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border CornerRadius="5" BorderThickness="1" BorderBrush="DarkGray" Background="WhiteSmoke" Padding="2" Cursor="Hand" >
                                <TextBlock Text="{Binding DisplayName}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsSelected" Value="true" />
                            <!--<Condition Property="Selector.IsSelectionActive" Value="true" />-->
                        </MultiTrigger.Conditions>
                        <Setter Property="Template" Value="{StaticResource SelectedTemplate}" />
                    </MultiTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <!--Textblocks at the top-->
        <TextBlock Text="Not in the collection" Grid.Column="0" Foreground="White" />
        <TextBlock Text="In the collection" Grid.Column="2" Foreground="White" />
        <!--Listviews that hold the folders. Left to Right-->
        <ListView x:Name="lv_Left" 
                  Grid.Row="1" 
                  Grid.Column="0"
                  Margin="5"
                  ItemsSource="{Binding OutOfCollection}" />
        <ListView x:Name="lv_Right" 
                  Grid.Row="1" 
                  Grid.Column="2" 
                  Margin="5" 
                  ItemsSource="{Binding InsideCollection}" />
        <!--Create Button-->
        <Button Content="Create" Grid.Column="1" Grid.Row="1"
                Margin="0 3 0 0"
                VerticalAlignment="Top"
                Style="{StaticResource SubmitButtonStyle}"
                IsEnabled="{Binding InsideCollectionHasItems}"
                Command="{Binding OpenConfirmationWindowCommand}" />
        <!--Left/Right Arrow Buttons-->
        <StackPanel Orientation="Vertical" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Grid.Column="1" Grid.Row="1">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                    <Setter Property="IsEnabled" Value="True" />
                    <Setter Property="Width" Value="40" />
                    <Setter Property="Height" Value="40" />
                    <Setter Property="Command" Value="{Binding SwapArrowCommand}" />
                </Style>
            </StackPanel.Resources>
            <Button>
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource CloneMultiConverter}">
                        <Binding ElementName="lv_Left" />
                        <Binding ElementName="lv_Right" />
                    </MultiBinding>
                </Button.CommandParameter>
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=lv_Left}" Value="-1">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image Source="pack://application:,,,/Icons/right_arrow.png" />
                </StackPanel>
            </Button>
            <TextBlock Focusable="False" IsEnabled="False" Height="10" Margin="0" />
            <Button>
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource CloneMultiConverter}">
                        <Binding ElementName="lv_Right" />
                        <Binding ElementName="lv_Left" />
                    </MultiBinding>
                </Button.CommandParameter>
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedIndex, ElementName=lv_Right}" Value="-1">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image Source="pack://application:,,,/Icons/left-arrow.png" />
                </StackPanel>
            </Button>
        </StackPanel>
        <!--Up/Down Arrow Buttons-->
        <StackPanel Orientation="Vertical" 
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Center"
                    Grid.Column="3" Grid.Row="1"
                    Margin="0 5 5 0">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                    <Setter Property="Height" Value="40" />
                    <Setter Property="Width" Value="40" />
                    <Setter Property="IsEnabled" Value="True" />
                    <Setter Property="Command" Value="{Binding UpDownArrowCommand}" />
                    <Setter Property="CommandParameter">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource CloneMultiConverter}">
                                <Binding Path="Tag" RelativeSource="{RelativeSource Mode=Self}" />
                                <Binding ElementName="lv_Right" />
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedIndex, ElementName=lv_Right}" Value="-1">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Resources>
            <Button Tag="Up">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image Source="pack://application:,,,/Icons/up-arrow.png" />
                </StackPanel>
            </Button>
            <TextBlock Focusable="False" IsEnabled="False" Height="10" Margin="0" />
            <Button Tag="Down">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image Source="pack://application:,,,/Icons/down-arrow.png" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
