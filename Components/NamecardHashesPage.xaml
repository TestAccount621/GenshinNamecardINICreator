<UserControl x:Class="GenshinNamecardINICreator.Components.NamecardHashesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenshinNamecardINICreator.Components"
             xmlns:models="clr-namespace:GenshinNamecardINICreator.Models"
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:GenshinNamecardINICreator.Converters" 
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:vr="clr-namespace:GenshinNamecardINICreator.ValidationRules"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontSize="18" Background="{StaticResource PrimaryBackground}">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"
                                                 True="Collapsed"
                                                 False="Visible" />

        <CollectionViewSource x:Key="NamecardsViewSource" Source="{Binding NamecardHashesPageComboBoxItemViewModels}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Namecard.Name" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        
        <Style x:Key="TopButtonsStyleBase" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="IsEnabled" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsReadOnly}" Value="False">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="TopButtonsStyleEditDelete" TargetType="Button" BasedOn="{StaticResource TopButtonsStyleBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedIndex, ElementName=cbxHashes}" Value="-1">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <vr:BooleanOrConverter x:Key="BooleanOrConverter" />
        <ControlTemplate x:Key="errorTemplateSilverlightStyle">
            <StackPanel Orientation="Horizontal">
                <Border BorderThickness="1" BorderBrush="#FFdc000c" CornerRadius="0.7"
                        VerticalAlignment="Top">
                    <Grid>
                        <Polygon x:Name="toolTipCorner"
                                 Grid.ZIndex="2"
                                 Margin="-1"
                                 Points="6,6 6,0 0,0" 
                                 Fill="#FFdc000c" 
                                 HorizontalAlignment="Right"                        
                                 VerticalAlignment="Top"
                                 IsHitTestVisible="True"/>
                        <Polyline Grid.ZIndex="3"
                                  Points="7,7 0,0" 
                                  Margin="-1"
                                  HorizontalAlignment="Right"
                                  StrokeThickness="1.5"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"
                                  Stroke="White"
                                  VerticalAlignment="Top"
                                  IsHitTestVisible="True"/>
                        <AdornedElementPlaceholder x:Name="adorner"/>
                    </Grid>
                </Border>
                <Border x:Name="errorBorder" Background="#FFdc000c" Margin="1,0,0,0"
                        Opacity="0" CornerRadius="1.5"
                        IsHitTestVisible="False"
                        MinHeight="24" MaxWidth="267">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="2.25" 
                                          Color="Black" 
                                          Opacity="0.4"
                                          Direction="315"
                                          BlurRadius="4"/>
                    </Border.Effect>
                    <TextBlock Text="{Binding ElementName=adorner,
                                      Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"
                               Foreground="White" Margin="8,3,8,3" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
            <ControlTemplate.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource BooleanOrConverter}">
                            <Binding ElementName="adorner" Path="AdornedElement.IsKeyboardFocused" />
                            <Binding ElementName="toolTipCorner" Path="IsMouseOver"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard x:Name="fadeInStoryboard">
                            <Storyboard>
                                <DoubleAnimation Duration="00:00:00.15"
                                                 Storyboard.TargetName="errorBorder"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="1"/>
                                <ThicknessAnimation Duration="00:00:00.15"
                                                    Storyboard.TargetName="errorBorder"
                                                    Storyboard.TargetProperty="Margin"
                                                    FillBehavior="HoldEnd"
                                                    From="1,0,0,0"
                                                    To="5,0,0,0">
                                    <ThicknessAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="2"/>
                                    </ThicknessAnimation.EasingFunction>
                                </ThicknessAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="fadeInStoryboard"/>
                        <BeginStoryboard x:Name="fadeOutStoryBoard">
                            <Storyboard>
                                <DoubleAnimation Duration="00:00:00"
                                                 Storyboard.TargetName="errorBorder"
                                                 Storyboard.TargetProperty="Opacity"
                                                 To="0"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0" Margin="10">
            <Button x:Name="btn_New" 
                    Content="New" 
                    Command="{Binding NewNamecardCommand}"
                    Style="{StaticResource TopButtonsStyleBase}" />
            <ComboBox x:Name="cbxHashes"
                      ItemsSource="{Binding Source={StaticResource NamecardsViewSource}}"
                      SelectedItem="{Binding SelectedNamecardHashesPageComboBoxItemViewModel}">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="IsEnabled" Value="False" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="Width" Value="300" />
                        <Setter Property="Margin" Value="10 0" />
                        <Setter Property="ItemsPanel">
                            <Setter.Value>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                <Setter Property="IsEnabled" Value="True" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ComboBox.Style>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Edit" Style="{StaticResource TopButtonsStyleEditDelete}" Command="{Binding UpdateNamecardCommand}" />
            <Button Content="Delete" Style="{StaticResource TopButtonsStyleEditDelete}" Margin="5 0 0 0" Command="{Binding DeleteNamecardCommand}" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Grid.Column" Value="0" />
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="Margin" Value="10 3 0 3" />
                    <Setter Property="Foreground" Value="White" />
                </Style>
                <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
                    <Setter Property="Grid.Column" Value="1" />
                    <Setter Property="Margin" Value="10 5" />
                    <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
                    <Setter Property="Height" Value="30" />
                    <Setter Property="Width" Value="90" />
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Grid.Column" Value="1" />
                </Style>
                <Style x:Key="NameTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource BaseTextBoxStyle}">
                    <Setter Property="Width" Value="300" />
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!--TextBlock labels on the left column.-->
            <TextBlock Text="Namecard Name:" Grid.Row="0" />
            <TextBlock Text="Main Hash:" Grid.Row="1" />
            <TextBlock Text="Preview Hash:" Grid.Row="2" />
            <TextBlock Text="Top Banner Hash:" Grid.Row="3" />
            <!--The Textboxes for input-->
            <TextBox x:Name="txtBox_Name" 
                     Grid.Row="0" 
                     Style="{StaticResource NameTextBoxStyle}"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Validation.ErrorTemplate="{StaticResource errorTemplateSilverlightStyle}"/>
            <TextBox x:Name="txtBox_Main"
                     Grid.Row="1" 
                     MaxLength="8"
                     Style="{StaticResource BaseTextBoxStyle}"
                     Text="{Binding MainHash, UpdateSourceTrigger=PropertyChanged}" 
                     Validation.ErrorTemplate="{StaticResource errorTemplateSilverlightStyle}"/>
            <TextBox x:Name="txtBox_Preview"
                     Grid.Row="2" 
                     MaxLength="8"
                     Style="{StaticResource BaseTextBoxStyle}" 
                     Text="{Binding PreviewHash, UpdateSourceTrigger=PropertyChanged}" 
                     Validation.ErrorTemplate="{StaticResource errorTemplateSilverlightStyle}"/>
            <TextBox x:Name="txtBox_Banner" 
                     Grid.Row="3" 
                     MaxLength="8"
                     Style="{StaticResource BaseTextBoxStyle}"
                     Text="{Binding BannerHash, UpdateSourceTrigger=PropertyChanged}" 
                     Validation.ErrorTemplate="{StaticResource errorTemplateSilverlightStyle}"/>
            <!--Save/Cancel Buttons-->
            <StackPanel Orientation="Horizontal" 
                        Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Top"
                        Margin="10"
                        Visibility="{Binding IsReadOnly, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button Content="Save" 
                        Height="{Binding ElementName=btn_New, Path=ActualHeight}" 
                        Style="{StaticResource SubmitButtonStyle}" 
                        Margin="0 0 20 0"
                        Command="{Binding SubmitCommand}"
                        IsEnabled="{Binding CanSubmit}">
                    <Button.CommandParameter>
                        <MultiBinding Converter="{StaticResource CloneMultiConverter}">
                            <Binding Path="Text" ElementName="txtBox_Name" />
                            <Binding Path="Text" ElementName="txtBox_Main" />
                            <Binding Path="Text" ElementName="txtBox_Preview" />
                            <Binding Path="Text" ElementName="txtBox_Banner" />
                        </MultiBinding>
                    </Button.CommandParameter>
                </Button>
                <Button Content="Cancel" 
                        Height="{Binding ElementName=btn_New, Path=ActualHeight}" 
                        Style="{StaticResource BaseButtonStyle}"
                        Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
